import 'package:flutter/material.dart';

class ChatDatail extends StatelessWidget {
  const ChatDatail({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("채팅방")],
    );
  }
}
